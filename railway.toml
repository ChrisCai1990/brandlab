[build]
builder = "dockerfile"

[deploy]
startCommand = "prisma db push --skip-generate && node prisma/seed.js; next start"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
